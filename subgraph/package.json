{
  "name": "aitu-gamefi-subgraph",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:studio": "graph deploy --studio aitu-gamefi",
    "create:local": "graph create --node http://localhost:8020/ aitu-gamefi",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 aitu-gamefi"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "latest",
    "@graphprotocol/graph-ts": "latest"
  },
  "devDependencies": {}
}
